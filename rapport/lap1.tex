\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{datetime}
\usepackage{subcaption}
\usepackage[letterpaper, margin=1.9cm]{geometry}
\usepackage{graphicx}
\usepackage{epstopdf}
\usepackage{siunitx}
\sisetup{output-decimal-marker = {,}}
\graphicspath{ {../figures} }
\usepackage{derivative}
\usepackage{booktabs}
\usepackage[flushleft]{threeparttable}
\usepackage{amsmath}

\usepackage{csquotes}
\usepackage[
backend=biber,
style=ieee,
%style=verbose,
citestyle=ieee,
%sorting=nyt,
%url=false
]{biblatex}
\bibliography{references}

\title{MEC6616 --- LAP1}


\author{Aurélie Grebe\\Noémie Herbinière}
\newdate{duedate}{31}{08}{2026}
\date{\displaydate{duedate}}

\begin{document}
\maketitle

\section{Introduction}

Dans la présente œuvre, nous proposons une résolution de l'équation de diffusion
de la chaleur par volumes finis telle que présentée par Versteeg et
Malalasekera\,\cite{versteeg_introduction_2007}. Après une brève explication de
la méthode de résolution, nous analyserons les résultats à l'aide de trois exemples.

\section{Méthodologie}

La méthode utilisée pour implémenter ce solveur provient directement du chapitre
4 de Versteeg et Malalasekera\,\cite{versteeg_introduction_2007}. Le solveur
que nous présentons ici résout l'équation de transmission de chaleur en 1-D:
\begin{equation}
	\odv{}{x}\left( k\odv{T}{x}\right) + S = 0
\end{equation}
avec $k$ la conductivité thermique et $S$ le terme source.

On appliquant une différence centrale pour évaluer des termes diffusifs et
présumant $k$ constant sur tout le domaine du problème, on arrive avec
une équation de forme suivante pour chacune des cellules de notre problème:
\begin{equation}
	a_p T_p = a_w T_w + a_e T_e + S_u
\end{equation}
Le sous-script $_p$ indique une valeur appartenant à la cellule même, tandis que
les sous-scripts $_e$ et $_w$ indiquent les cellules à l'est et à l'ouest
respectivement. $S_u$ est le terme source uniforme, c'est à dire qui ne dépend
pas de la température. Pour les cellules intérieures, $a_w = a_e =
\frac{kA}{\delta x}$, et $a_p = a_w + a_e - s_p$. Si on prend des valeurs 
de sources$q_u$ et $q_p$ avec les unités 
\footnote{On choisit ces unités, car d'habitude
la partie uniforme est due à une génération de chaleur volumique, p. ex.
chauffage résistif, tandis que la partie proportionnelle provient d'un transfert
de chaleur avec l'environnement qui peut être exprimé en unités linéaires dans un 
cas 1-D.}
\unit{\watt\per\m\cubed} et \unit{\watt\per\m\per\celsius}, on obtient les
termes sources uniformes
$S_u = q_uA/\delta x$ et proportionnel $S_p = q_p \delta x$.

Pour les cellules en frontière, leur traitement dépend du type de condition à 
la frontière imposé. Pour des frontières de Dirichlet où on impose une valeur
fixe, on ajoute à $S_u$ le terme $2kAT_f/\delta x$ et à $S_p$ le terme
$2kA/\delta x$, où $T_f$ est la température à cette frontière. À une frontière
de type Neumann où on impose plutôt le gradient de température, on ne fait
qu'ajouter à $S_u$ le terme $T'_fA$. Un résumé de tous ces termes se trouve au
tableau \ref{tab:coeffs}.

\begin{table}
	\centering
	\begin{threeparttable}
	\caption{Coefficients pour l'équation de chaleur discrétisé}
	\label{tab:coeffs}
	\begin{tabular}{@{}rccccc@{}}
		\toprule
		& $a_w$ & $a_e$ & $S_u$ & $S_p$ & $a_p$ \\\midrule
		Intérieur & $\frac{kA}{\delta x}$ & $\frac{kA}{\delta x}$ & $q_uA\delta x$ & $q_p \delta x$ & $a_w + a_e - S_p$ \\
		Direchlet &  & $\frac{kA}{\delta x}$ & $q_uA\delta x + \frac{dkAT_f}{\delta x} $ 
			  & $q_p \delta x + \frac{dkA}{\delta x}$ & $a_w + a_e - S_p$ \\
		Neumann &  & $\frac{kA}{\delta x}$ & $q_uA\delta x + T'_fA$ & $q_p \delta x$ & $a_w + a_e - S_p$ \\\bottomrule
	\end{tabular}
	\begin{tablenotes}
		\item\textdagger Seules les conditions de frontières à gauche sont
		montrées. Pour celles à droite, il faut simplement échanger
		$a_e$ et $a_w$
	\end{tablenotes}
	\end{threeparttable}
\end{table}

Pour résoudre ce système de $N$ équations, on formule une équation matricielle de
la forme $Ab = S$:

\begin{equation}
	\begin{bmatrix}
		-a_{p1} & a_{e1} & 0 & \dots & 0 & 0 \\
		a_{w1} & - a_{p2} & a_{e2} & \dots & 0 & 0 \\
		\vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\
		0 & 0 & 0 & \dots & a_{wN} & -a_{pN}
	\end{bmatrix}
	\begin{bmatrix}
		T_1 \\
		T_2 \\
		\vdots \\
		T_N 
	\end{bmatrix}
	=
	\begin{bmatrix}
		S_{u1} \\
		S_{u2} \\
		\vdots \\
		S_{uN} 
	\end{bmatrix}
\end{equation}


\section{Résultats}
\subsection{Exemple 4.1}

L'exemple 4.1 de Versteeg et Malalasekera est un problème stationnaire de transfert de
chaleur d'une barre isolée dont la température sur un bord est fixée à
\SI{100}{\celsius} et l'autre à \SI{500}{\celsius}. Il s'agit donc des
conditions aux limites de Dirichlet.

Les solutions calculées à l'aide de la méthode par volumes finis (FVM) sont
comparées en figure~\ref{fig:ex4_1} avec la solution analytique :
\begin{equation}
	T(x) = 800x + 100
\end{equation}
On peut voir que puisqu'il s'agit d'une solution linéaire et que notre schéma
utilise une interpolation linéaire de la température, le résultat numérique est
exact dans le degré de précision accordé par la représentation des nombres à
point flottant (figure~\ref{fig:err4_1}). Calculer un ordre de convergence n'a
donc pas de sens pour ce cas particulier.


\begin{figure}[htb]
	\centering
	\begin{subfigure}[t]{0.45\textwidth}
		\includegraphics[width=\textwidth]{ex4_1.png}
		\caption{Résultats analytiques et par volumes finis de l'exemple 4.1}
		\label{fig:ex4_1}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.45\textwidth}
		\includegraphics[width=\textwidth]{epsilon4_1.png}
		\caption{Erreur de solution selon la finesse de discrétisation
		spatiale.}
		\label{fig:err4_1}
	\end{subfigure}
	\caption{}
\end{figure}

\subsection{Exemple 4.2}

L'exemple 4.2 de Versteeg et al. est un problème stationnaire de transfert de
chaleur d'une plaque épaisse dont la température d'une face est fixée à $T_A =
\SI{100}{\celsius}$, la face opposée à $T_B = \SI{200}{\celsius}$ et où une
source de chaleur uniforme $q = \SI{1000}{\kilo\watt\per\m\cubed}$ est
appliquée. Les températures fixées sur les faces sont donc les conditions de
Dirichlet. On considère que les dimensions dans les directions y et z sont si
grandes que seul un gradient de température dans la direction x est vraiment
signifiant. Le problème peut donc être assimilé à un problème 1D. 

Les solutions calculées à l'aide de la méthode par volumes finis (FVM) sont
comparées en figure~\ref{fig:ex4_2} avec la solution analytique (fournie par 
l'équation 4.39 du livre de Versteeg) :
\begin{equation}
	T(x) = \left(\frac{T_B-T_A}{L} + \frac{q}{2k} (L-x)\right)x + T_A
\end{equation}

On peut observer que les points obtenus par FVM suivent bien la courbe analytique
et que, comme attendu, plus le nombre de points augmente, plus les résultats sont 
proches du résultat analytique, car on gagne en précision. Pour cet exemple, la 
méthode FVM tend à surestimer la température pour un faible nombre de points.
Dès 20 points de discrétisation, l'approximation est très proche de la solution analytique.

La figure~\ref{fig:err4_2} montre l'erreur RMS en fonction de la taille de
cellule $h$. Affiché sur une échelle logarithmique, on observe une pente de
\num{2.00}, indiquant une convergence observée d'ordre 2, ce qui correspond aux attentes.

\begin{figure}[h]
	\centering
	\begin{subfigure}[t]{0.45\textwidth}
		\includegraphics[width=\textwidth]{ex4_2.png}
		\caption{Résultats analytiques et par volumes finis de l'exemple 4.2}
		\label{fig:ex4_2}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.45\textwidth}
		\includegraphics[width=\textwidth]{epsilon4_2.png}
		\caption{Évolution de l'erreur en fonction du nombre de points $N$ (ici h=1/N), à l'échelle logarithmique, de l'exemple 4.2}
		\label{fig:err4_2}
	\end{subfigure}
	\caption{}
\end{figure}

\subsection{Exemple 4.3}

L'exemple 4.3 de Versteeg et al. est un problème de refroidissement par convection 
d'une barre à section cylindrique dans un environnement à une température ambiante 
de $T_\infty = \SI{20}{\celsius}$ . Une extrémité est fixée à $T_B = \SI{100}{\celsius}$ (condition 
de Dirichlet) tandis que l'autre est isolée (condition de Neumann où le
transfert de température est nul, $q"_a = 0$). La section de la barre étant 
constante, le problème est considéré 1D.

Les solutions calculées à l'aide de la méthode par volumes finis (FVM) sont
comparées en figure~\ref{fig:ex4_3} avec la solution analytique (fournie par 
l'équation 4.41 du livre de Versteeg) :
\begin{equation}
	T(x) = (T_B - T_\infty) \frac{\cosh(n(L - x))}{\cosh(nL)} + T_\infty
\end{equation}

Avec $n^2 = \frac{(hP)}{kA} = 25 m^{-2}$. 

Les équations étant exprimées en fonction de n plutôt que k et q, les valeurs de 
k et A ont été fixées à l'unité afin d'accorder la formulation utilisée dans le 
solveur et celle utilisée dans l'exemple 4.3. Les $Q_U$, $Q_P$ ont été définis grâce 
à la table fournie dans le livre ($Q_U = n^2T_\infty$; $Q_P = -n^2$).

Tout comme l'exemple 4.2, on observe que les points obtenus par FVM suivent la 
courbe analytique et gagnent en précision quand le nombre de points augmente. 
Cette fois-ci, la méthode FVM a tendance à sous-estimer la température par rapport 
à la solution analytique. Cependant, encore une fois, il suffit de 20 points de 
discrétisation pour obtenir une approximation très proche de la solution analytique.

La figure~\ref{fig:err4_3} montre l'erreur RMS en fonction de la taille de
cellule $h$. Affiché sur une échelle logarithmique, on observe une pente de
\num{2.00}, indiquant une convergence observée d'ordre 2, ce qui est exactement ce qui était attendu.

\begin{figure}[h]
	\centering
	\begin{subfigure}[t]{0.45\textwidth}
		\includegraphics[width=\textwidth]{ex4_3.png}
		\caption{Résultats analytique et par volume finis de l'exemple 4.3}
		\label{fig:ex4_3}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.45\textwidth}
		\includegraphics[width=\textwidth]{epsilon4_3.png}
		\caption{Évolution de l'erreur en fonction du nombre de points $N$ (ici h=1/N), à l'échelle logarithmique, de l'exemple 4.3}
		\label{fig:err4_3}
	\end{subfigure}
	\caption{}
\end{figure}

\printbibliography[
	heading=bibintoc,
	title={Références}
]
	
\end{document}
